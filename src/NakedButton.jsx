import styled from "styled-components";

export default styled.button`
  padding: 0;
  border: none;
  background: none;
  cursor: pointer;
  color: inherit;
  font-weight: inherit;
  font-size: inherit;
`;
